# AST Generation Utilities

### gen_ast.dart
This utility generates ast files for the OcheScript embedded language.

### gen_bytecode_vm_ast.dart
This utility generates ast files for the Bytecode VM runtime.

`gen.bat` is the batch that will run the `gen_bytecode_vm_ast.dart` utility and place the generated files in the correct location.
